<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.baidu.retrofit">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:appComponentFactory=""
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/TranslucentTheme"
        tools:replace="android:appComponentFactory">
        <activity android:name=".Activity.ImageSwitcherActivity"></activity>
        <activity android:name=".Activity.wanAndroid.HomePageActivity" />
        <activity android:name=".Activity.CustonViewBserActivity" />
        <activity android:name=".Activity.ImageListActivity" />
        <activity android:name=".Activity.LoadMoreActivity" />
        <activity android:name=".Activity.CustomViewActivity" />
        <activity android:name=".Activity.LoginActivity" />
        <activity android:name=".Activity.SecondActivity" />
        <activity android:name=".Activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity.FirstActivity" />
    </application>

</manifest>